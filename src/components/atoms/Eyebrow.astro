---

interface Props {
  text: string;
  tone?: 'ink' | 'nav';
}

const { text, tone = 'ink' } = Astro.props;

const toneClass = tone === 'nav' ? 'text-nav-muted' : 'text-ink-400';
---

<p
  class:list={[
    'font-mono text-[11px] tracking-[0.18em] uppercase',
    toneClass,
  ]}
>
  {text}
</p>
